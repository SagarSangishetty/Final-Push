apiVersion: jaegertracing.io/v1
kind: Jaeger

metadata:
  name: jaeger
  namespace: traces

spec:
  strategy: production

  collector:
    replicas: 1

  query:
    replicas: 1

  storage:
    type: elasticsearch

    options:
      es:
        server-urls: https://elasticsearch-master:9200
        username: elastic
        password: fVua91b93FwcVE1q
        tls:
          insecureSkipVerify: true