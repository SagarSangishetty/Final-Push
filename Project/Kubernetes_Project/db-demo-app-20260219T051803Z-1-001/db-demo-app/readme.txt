Application Info -- This is a simple application which stores email front end as node and backend as mongodb to store email and display

Use Case : This application runs on app and db server 
		We can deploy this on same pod (No recommended)
		We can deploy this in 2 similar pods (Production type) using volume PV & PVC