apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: urlshortener-pvc
  namespace: urlshortener
spec: 
  volumeName: urlshortener-pv
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests: 
      storage: 1Gi