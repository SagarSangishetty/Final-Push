git branch -- to check current branch

git branch <branch name> -- to create a new branch

git merge <branch main> -- to meregr the branch 

Fork in git --- Copy of someone else’s repository

git ignore -- to ignore creatin files from commit we add file name in this file.

git clean -- remove untracked file

git clean -n -- shows which file is to be removed.

git clean -f -- removes untracked files

--- In Git, a tag is like a label or marker that points to a specific commit — usually used to mark release

git tag --- List all tags

git tag v1.0 --- Create a tag

git tag v1.0 <commit-hash> --- Tag a specific commit

git push origin v1.0 --- git push origin v1.0

git push origin --tags --- Push all tags

git tag -d v1.0 --- Delete locally

git push origin --delete v1.0 --- Delete from remote (GitHub)